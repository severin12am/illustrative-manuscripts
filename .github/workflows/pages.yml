name: Deploy GitHub Pages

on:
  push:
    branches: [main, cursor/illustrative-manuscripts-scaffold-c6d7]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - run: npm ci

      - name: Generate witness data
        run: node scripts/generate-data.mjs
        continue-on-error: true

      - name: Build static site
        env:
          NEXT_PUBLIC_BASE_PATH: /illustrative-manuscripts
        run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
